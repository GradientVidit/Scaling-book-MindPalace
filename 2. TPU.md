Tensor Processing Units (TPUs) are Google's custom-developed ASICs used to accelerate machine learning workloads.

Single Chip Architecture : [[Single TPU Chip Architecture]]
Multiple TPU chips interaction Paradigm : [[TPU Symphony]]
Network Bandwidths and Hierarchy among TPUs: [[Network BS]]

---
### Notes

- **Weight matrices need to be padded to at least size 256 on TPU v6e in both dimensions to fill up the MXU (in fact, smaller axes are padded).
    
- **Lower precision matrix multiplication tends to be faster.** TPUs can do int8 or int4 OPs roughly 2x/4x faster than bfloat16 FLOPs for generations that support it. VPU operations are still performed in fp32.
    
- To avoid bottlenecking the TPU compute unit, we need to **make sure the amount of communication across each channel is proportional to its speed**.

